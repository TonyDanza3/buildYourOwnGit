package constant;

import java.util.Formatter;

public class ErrorMessage {
    public static final String INVALID_COMMAND = "git: '%s' is not a git command. See 'git --help'.";
    public static final String NOT_A_GIT_REPOSITORY = "fatal: not a git repository (or any of the parent directories): .git";
}
